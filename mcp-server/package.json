{
  "name": "mcp-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/server.js",
  "types": "dist/types.d.ts",
  "scripts": {
    "build": "tsc -p .",
    "dev": "node --loader ts-node/esm src/server.ts",
    "start": "node dist/server.js",
    "test": "vitest run"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.4.0",
    "ts-node": "^10.9.2",
    "vitest": "^1.6.0"
  }
}
